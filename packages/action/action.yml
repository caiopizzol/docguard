name: 'DocGuard'
description: 'Detect documentation regressions'
inputs:
  config:
    description: 'Path to config file'
    default: 'docguard.yml'
runs:
  using: 'composite'
  steps:
    - run: npx docguard check --format=json
      shell: bash
